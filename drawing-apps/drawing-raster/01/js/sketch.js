
function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255);
  noStroke();
}


function draw() {
  // Apply color to fill and draw
  fill(0, 10);
  circle(mouseX, mouseY, 100);
}
